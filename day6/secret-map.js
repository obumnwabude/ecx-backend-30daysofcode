/** 
 * Returns the squares of elements in an array 
 * @author Obumuneme Nwabude <obumnwabude.com>
 * @param {Array.<number>} toSquare - the array of numbers to square its members
 * @return {Array.<number>} - the array of squared numbers
 */
const secretMap = toSquare => toSquare.map(value => Math.pow(value, 2));