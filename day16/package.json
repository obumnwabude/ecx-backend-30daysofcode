{
  "name": "auth-auth",
  "version": "1.0.0",
  "description": "A server that takes in username, email and password from a SIGNUP route to create a user, and has a LOGIN route to authenticate the user using the password and the email or username.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Obumuneme Nwabude",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
