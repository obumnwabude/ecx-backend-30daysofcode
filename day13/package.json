{
  "name": "express-roads",
  "version": "1.0.0",
  "description": "a server that takes JSON from a POST request and returns the same data as a response from a GET request on another route",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Obumuneme Nwabude",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
